import { createMuiTheme } from "@material-ui/core/styles";

const theme = createMuiTheme({
  palette: {
    primary: {
      main: "#3498db" //blue for PleaseButton2
    },
    secondary: {
      main: "#2ecc71" //green for TestButton2
    }
  }
});

export default theme;
